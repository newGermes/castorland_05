'use strict';
var Cart = (function() {
        var getString = function(parametr) {
                return parametr;
            },
            item = 1;

        return {
            getString: getString,
            item: item
        };
} ());