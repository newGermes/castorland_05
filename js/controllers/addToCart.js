'use strict';
var addToCart = (function() {
    var delegation = function(parent, child) {
        if (parent && child) {
            
        }
    };

    return {
        delegation: delegation
    };
}());